language: node_js

node_js:
  - 10

cache: yarn

script:
  - "storybook:serve &"
  - "yarn lint"
  - "yarn test:unit --coverage"
  - "storybook:build"
  - "yarn test:pupp"
  - "yarn build"
  - "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js"
